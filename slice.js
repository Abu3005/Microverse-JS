// You can use the following code to get the first two characters of a string:
 
const firstTwo = string.slice(0,2);
 
// Use this for the last two:
 
const lastTwo = string.slice(-2);
 
// And use this to convert a string to an integer:
 
const intNum = parseInt(firstTwo);
